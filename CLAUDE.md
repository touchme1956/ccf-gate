# CCF Ω 壊れない複利の門 — パイプライン作業場

個人投資家たっちみーさんの銘柄審査パイプライン。20-30年の長期複利が目的。
**index.html が門（UI・台帳・売却規律・審査プロトコルの正本）**。GitHub Pagesで自動配信される（2026-07移行。公開URL: touchme1956.github.io/ccf-gate/index.html）。

## ⚠ 文脈の窓を溢れさせない読み方（2026-09-21・autocompact の空回りを止めた）

**症状**: compact 直後3ターンで窓が満杯になるのが3回続いた。原因は会話の長さではなく**1回の読みの大きさ**だった。
実測（`python3 night/ctx_report.py`）——
`index.html` を Read の**既定(2000行)で開くと1回で概算 81,800 tok**（窓のおよそ4割）・全文なら 288,700 tok。
`out/hist10_angleC.json` は 2,443,800 tok で**そもそも窓に入らない**。25,000 tok を超えるファイルは**169件**ある。
CLAUDE.md の床は **32,000 tok（窓の16%）**あり、既定 Read を2回やればそれだけで compact が起きていた
（この日の切り出しで床は **14,500 tok・窓の7%** になった。数え直しは `python3 night/ctx_report.py`）。

- **止める仕掛け**: `.claude/hooks/ctx_guard.py`（PreToolUse）が **Read と `cat` の全文読みだけ**を止め、代わりの叩き方を返す。
  上限 25,000 tok。判定に使うのはファイルの大きさだけで中身は見ない。書き込み・実行・採点には触れない。
  逃げ道は `CCF_CTX_GUARD=off`。**止まったら諦めるのではなく、下の覗き窓で当てること**（読まないのは事故のもと）。
- **覗き窓**: `python3 night/peek.py`——大きいファイルを刻んで読む単一の道具。
  ```bash
  python3 night/peek.py map   index.html              # まず目次（行番号・区間の大きさ）
  python3 night/peek.py find  index.html ccfAllocTop  # 関数・id の定義区間だけ
  python3 night/peek.py grep  index.html 'moatIdx'    # 一致行だけ（46,650字の行は切って出す）
  python3 night/peek.py lines index.html 6042 6067    # 行範囲だけ（sed -n '6042,6067p' と同じ）
  python3 night/peek.py json  out/score_all.json      # JSONは「形」だけ見て、値は jq で絞る
  python3 night/peek.py size  <ファイル…>              # 読む前に文脈コストを見積もる
  ```
- **index.html を直すとき**: 全文を読まない。`map`→`find`で**その関数の区間だけ**を読み、Edit は文字列置換で当てる
  （Edit は読んだ範囲だけで成立する）。門の版番号・意匠の一斉置換は `night/` の道具でやる。
- **out/ の JSON**: `cat` しない。`jq` で絞るか `node night/score_all.js` 等の集計器の出力を読む。
- **計測**: `python3 night/ctx_report.py [--all]` が床・危険ファイル・ガードの結線を実測する（判定には不使用）。

## 記録の索引（全文は docs/CLAUDE_ARCHIVE.md）

**記録は消していない。**日付・版番号つきの 245節（約511,216 tok）を `docs/CLAUDE_ARCHIVE.md` へ**移した**だけ。
ここに残したのは運用の節（コマンド・絶対のルール・月次運用・別枠の門・並走運用）と、この索引。
**段階2（同日）**でさらに、運用の節の中に残っていた**経緯・事故の顛末 10塊（約20,150 tok）**も書庫へ移した
（コマンドの叩き方・刻み・閾値・規約はすべてここに残してある）。移した先は**その場に置いた `→ docs/CLAUDE_ARCHIVE.md L####` の1行**が名指しする
——この索引と合わせて、書庫の全 255節に索引が付いている。
**段階3（2026-09-21・autocompact の空回り対策）**でさらに二つ切り出した——**索引表そのもの**を
`docs/CLAUDE_ARCHIVE_INDEX.md` へ、**コマンド節の長い22節（経緯・実測・踏んだ罠）**を
`docs/CLAUDE_COMMANDS.md` へ。CLAUDE.md には**叩き方と刻み・閾値の1行索引**と
`→ docs/CLAUDE_COMMANDS.md L###` の行番号が残っている。床 32,002→14,505 tok。**消した行は1行も無い**
（検算: 旧CLAUDE.mdの非空行886行が、新CLAUDE.md・書庫・索引表・コマンド詳細のどれかに全部在る）。

⚠ **索引に載っている節は、必要になったら必ず読むこと。**「書いていない」ではなく「別のファイルに在る」。
**索引表（255節ぶん）は `docs/CLAUDE_ARCHIVE_INDEX.md`**（床を軽くするため2026-09-21に切り出した。記録は消していない）。
```bash
grep -n '^## ' docs/CLAUDE_ARCHIVE.md | grep 'キーワード'   # 探す（索引表を読まずに直接当たる）
sed -n '1234,1400p' docs/CLAUDE_ARCHIVE.md                  # 行番号で読む
awk '/^## /{p=0} /^## .*キーワード/{p=1} p' docs/CLAUDE_ARCHIVE.md  # 節ごと読む
```
## コマンド
- **文脈の実測とガード（2026-09-21新設）**: `python3 night/ctx_report.py`（床・危険ファイル・ガードの結線）／`python3 night/peek.py`（大きいファイルの覗き窓）／`.claude/hooks/ctx_guard.py`（Read と cat の全文読みだけを止める）。詳細は上の「⚠ 文脈の窓を溢れさせない読み方」
- 年1回（1-2月）発掘: `python run_gate0_local.py`
  - companyfacts.zip(1.4GB)が45日超なら自動で最新に更新（検証母集団=全上場が毎年更新される）
  - holdings.json があれば HOLDINGS/WATCH を自動差し替え（門の「Ⅳ台帳」の保有と揃えて用意）
  - 出力: gate1_queue.json（待ち行列＝TOP_N100＋審査優先〔谷/種まき/未成熟〕の合流で約155社）、gate0_all.csv
- 年1回 日本株発掘（日本株門0・二系統）: `python3 night/rebuild_gate0_jp.py --write` → キュー＝**pt上位50 ∪ 質の椅子(opm≥35 ∧ eq≥75 ∧ cagr≥5)**。pt=0.4615*min(cagr,30)+0.3846*min(opm,40)+0.1538*min(eq,80)＝**roicは入れない**（生ROICは審査後と中央値5.01倍ずれる）。別系統 `python run_gate0_jp_local.py`＝EDINET API直採取。ふるいの条件変更はユーザー明示指示の領分 → **docs/CLAUDE_COMMANDS.md L14**
- 四半期 保有点検: `python kessan_check.py`
  - holdings.jsonの保有銘柄について、直近の10-Q/8-Kを確認し、四半期売上YoY・営業利益率の前年同期差・警報(誠/限/集/指針/減損/退任)を機械抽出
  - 出力: out/kessan/{T}_qcheck.txt と要審査フラグ。要審査は門2再審査(依頼文)へ回す。株価は判定に使わない
- 四半期 決算カレンダー: `python kessan_calendar.py`
  - 監視リスト(保有+質80+)の次回決算日を取得(Alpha Vantage、鍵なしはSEC推定)
  - 出力: kessan_calendar.ics(Googleカレンダー取込=スケジュール連動) と out/next_earnings.json(決算日データ。門内の常時表示は撤去→Googleカレンダーで確認)
- 四半期 日本株点検: `python3 kessan_check_jp.py`（2026-07-29新設）
  - kessan_check.py / kessan_calendar.py は**SEC経由なので日本株を一切点検できず穴になっていた**。
    EDINET直配信の有報PDFから警報語（誠/限/集/指針/減損/退任の日本語版）を走査する。
    **数値抽出(売上YoY・営業利益率差)はPDFの桁分断でほぼ機能しない**ため、パックのgmを錨に自己検算し
    乖離が大きければ数値を破棄する（もっともらしい誤値より空欄）。判定は警報のみで行う
- 監視リスト生成: `python3 make_kanshi.py`（`--dry`で差分だけ）
  - 定義は **保有 ∪ 投下可 ∪ Ω75+ ∪ 直近点検で要審査 ∪ irr=85（特別監視枠） ∪ pin**
    （2026-07-29改定・**2026-08-09に irr=85 を追加**）。
    旧定義「保有+質80+」は採点の是正でΩ80+が4社へ収縮したとき監視が7社になり、
    KLACの$230.4百万のれん減損（当時Ω79.8）を見逃す状態だった。
    **要審査の社を必ず残す**のが肝——Ω75+だけで引くと、異常を拾った社が翌四半期に監視から消える
- 四半期の手順書は kessan_checklist.md、監視リストの正本は kanshi_list.json（make_kanshi.pyで生成）、Ⅶ資産の中身は portfolio.html
- 夜間バッチ審査は night/（chunkNN.txt=銘柄リスト、agent_prompt_template.txt=審査官指示〔正本はⅡ手順3・改定時は同期〕、
  progress.json=進行表。出力は out/{T}_gate_pack.json。日本株の一括再審査は agent_prompt_template_jp.txt〔EDINET_DB
  で自力採取・JP必須規約強制〕にコード列を渡す）。詳細は night/README.md
- **門へ反映する手順（3手。どれか1手では届かない）**: ①ハード再読み込み ②Ⅳ台帳「↻全再採点」＝採点ロジック・閾値の変更はここで効く ③Ⅵ「⭳ 全パック一括取込」＝パックのデータ自体の是正はここでしか届かない。パックを新規追加したら索引の再生成が要る（`python3 night/make_chunks.py` が末尾で更新） → **docs/CLAUDE_COMMANDS.md L76**
- **門の中の全件点検（v9.9.55）**: Ⅳ台帳の 🔍 全件点検＝`ccfAudit(d,r,coerce)`（純関数・採点に不介入）を全銘柄に掛け **要修正／警告／測定漏れ**の3階層で出す。空欄は `best`(測らないほうが点が高い)／`pena`／`stop`／`ok` の4種に分類する → **docs/CLAUDE_COMMANDS.md L89**
- **eps欄の定義が台帳内で二つに割れている（TTM実績と通期実績の混在）**: 決着は「**統一しない**。代わりに価格更新を基準保存式 `per_new = per_old × (px_new÷px_old)` にした」。eps は compute() で一切使われない参考値＝evidenceに基準を明記して運用する → **docs/CLAUDE_COMMANDS.md L120**
- **バッジ潰し**: `node night/audit_gate.js`（`--all` / `--k acq5,per` / `--t MCO`）＝門の `ccfAudit` をそのまま全パックへ掛け、✓検算済と未解決を分ける。**第四の関門**＝点検が鳴っている社は買付の土俵から降ろす（門は要修正errだけ／端末は未解決warnまで・線を二層で違える） → **docs/CLAUDE_COMMANDS.md L144**
- **📊 盤（ダッシュボード・v9.9.58）**: 門の先頭タブ。配管は GitHub Actions（`.github/workflows/market.yml` → `night/fetch_dashboard.py` → out/dashboard.json、鍵は Secrets の `FINNHUB_KEY` でブラウザに出ない）。**判定には一切使わない**。日本株はFinnhub無料枠外＝「価格未取得」と明示する → **docs/CLAUDE_COMMANDS.md L166**
- **E[r]の予実台帳**: `python3 night/audit_er_realized.py`（`--snap` で観測を封じる / 引数なしで突合せ）→ out/er_ledger.json。月初にCIが自動でsnap。**180日未満は年率換算しない**／**価格リターンに配当は入らない**（揃えずに結論すると自分で作った偏りを発見と誤認する） → **docs/CLAUDE_COMMANDS.md L187**
- **時間軸の薄さの計測**: `python3 night/shadow_time_axis.py` / `night/shadow_tc_roic.py` → 判定帯で唯一ばらつく数字は roic（変動係数64.0% 対 p4 7.9%）。重みではなく **through-cycle 化**で直した（規約は下の 7-c）。在庫 out/tc_roic.json → **docs/CLAUDE_COMMANDS.md L207**
- 採点の配分の検査: `node night/audit_weights.js`（`--q75`）→ 各欄を振って**対数弾力性**を出し、
  **名目の重みと実効の重み**の差を実測する。入れ子の幾何平均では「重み.22」と書いてあっても
  同じ項が複数階層に居れば経路が足し算になる（堀は意図して二経路／Fは意図せず二経路だった＝v9.9.67で解消）。
  **必ず `--q75` で判定帯を見ること**——全社の中央値は低Ω帯（線形の柱ペナルティ）に支配されて嘘をつく
- 日本株を米国規約へ揃える影の計測: `python3 night/shadow_jp_us_roic.py`（`--write`で反映）→ JP規約(NOPAT=営利×0.70固定・
  控除はのれんのみ・60%上限)と米国規約(実効税率・のれん＋無形控除・上限なし・IC縮退ガード)の差を出す。
  投下資本は**パックのroic/roicgから逆算**するので有利子負債を再採取しない（EDINET_DBのJGAAP負債は不完全）
- 基準の混在の点検: `python3 night/audit_basis.py`（`--list`）→ `_meta.basis.roic`(through-cycle/single-year/na)と
  `_meta.basis.roicConvention`(us-unified/jp-legacy)の2軸を数える
- 全パック検証: `node night/score_all.js` → 門の compute()／ccfXJudge／ccfMoatGate をそのまま走らせて
  Ωと**三段関門の合否（🟢投下可が何社か）**を実測。`--jp/--us/--only/--set`。出力 out/score_all.json。
  「基準を変えたら誰がどう動くか」は推測でなくこれで出す
- 全件点検の端末版: `node night/audit_gate.js`（`--all` / `--k acq5,per` / `--t MCO`）→ 門の `ccfAudit` を
  そのまま全パックに掛け、項目別の残数と銘柄別の内訳を出す。**✓検算済**（_metaに根拠あり＝門が_metaを
  読めないため仕様どおり鳴っているだけ）と**未解決**を分けるので、作業リストが本物だけになる。
  ブラウザの 🔍 全件点検 と同じ関数を読む（二重実装を作らない）
- 堀の根拠検査: `python3 night/audit_moat.py`（`--q75` / `--list`）→ dom/moatWの根拠の質を仕分けて
  再監査の作業リストを出す。**堀のふるいは入力の質を超えられない**ので、審査の納品後に必ず回す
- 堀のふるいの残り幅: `python3 night/audit_moat_gap.py`（`--all` / `--list`）→ 堀が関門に届かない社ごとに、規約の刻みで**単独昇格すれば通る本**を出す＝原本を読む先を絞る道具（audit_moat は「測っているか」、こちらは「読んでも合否が動くか」） → **docs/CLAUDE_COMMANDS.md L228**
- ROICの分母縮退検査: `python3 night/audit_roic.py`（`--q70` / `--list`）→ のれん除外ROICで投下資本が
  自己資本のごく一部まで縮退し発散している疑いのある社を仕分ける（絶対のルール7の検査器）。
  **高ROIC自体は異常でない**（roic=事業の質/roicg=買収規律の乖離は設計どおり）ので作業リストであって有罪判決ではない
- 根拠被覆率の検査: `python3 night/audit_evidence.py`（`--q75` / `--list` / `--t NVDA`）→ 「値は入っているが
  _meta.evidence に根拠が無い」欄を項目別に数える。**初回実測 33.0%（機械項目9.8% / 判断項目54.2%）**——
  原本から測った値とそれらしく置いた値が台帳上まったく同じ見た目になるため、誤りは人が根拠を読むまで
  見つからなかった。moatW だけ100%なのは規約が出来てから作られた項目だから。作業リストであって有罪判決ではない
- 納品検査: `python3 night/validate_packs.py {T}`（`--new` / `--summary`）→ **値があるのに根拠が無いパックを納品不可で落とす**。原本の鮮度（reportDate が auditDate から3年以上前）と `_meta` の型も検査する。夜間審査は納品前にこれを通しFAILゼロにすること → **docs/CLAUDE_COMMANDS.md L244**
- 機械項目の根拠の後追い: `python3 night/backfill_machine_evidence.py`（`--write` / `--sync`）→ 既存パックを
  採取器で再計算し、**一致した欄には式と実額の根拠を刻み、食い違う欄は書き換えず作業リストへ出す**。
  値を勝手に直さないのは、審査官が原本で検算して置いた値のほうが機械より強いから。
  fcf/ni は門が比でしか使わないので**比で判定**する（絶対値だと単位規約の差だけで全社が乗り、
  鳴りすぎる警報は鳴らないのと同じになる）
- acq5(直近5年の大型買収)の是正: `python3 night/fix_acq5.py`（`--write` / `--all`）→ のれん系列から機械判定（**yes≥30% / no<10% / 中間は空欄**＝「今あるのれんの何割が直近5年に入ってきたか」）。列挙欄に数値が入ると門が読めず罰が黙って効く → **docs/CLAUDE_COMMANDS.md L255**
- ROIIC³の機械算出: `python3 night/fill_roiic.py`（`--write`）→ **のれん込みIC**（自己資本+有利子負債）で算出。`'na'` は失敗ではなく「成熟還元型」（門はF6=58）。**迷ったら 'na' に倒す**——低い数字は複利停止キルを誤爆させる → **docs/CLAUDE_COMMANDS.md L267**
- 導出で決まる判断項目の整理: `python3 night/fill_derived_judgment.py`（`--write` / `--p1` / `--p3f5`）→ p3はnde・f5はdisruptの表で門が導出するので、同値なら空欄へ戻す（残すと陳腐化する）。**p1は変動係数(σ÷平均)の刻み: <15%→90 / <30%→80 / <50%→65 / 以上→50**（影の計測 `night/shadow_p1_cv.py`。刻みは index.html 本文／ccfAskText／night/agent_prompt_template.txt の3箇所を同期させること） → **docs/CLAUDE_COMMANDS.md L277**
- shtの充填: `python3 night/fill_sht.py`（`--impact` / `--write`）→ シェア趨勢を**同業比の売上成長**から機械算出。刻みは非対称（**down=−20%以上縮小 / up=+25%以上拡大**、acq5='yes' なら up にしない、中間は空欄）。母集団・対象とも**USD報告のみ**（名目成長は現地インフレを含む） → **docs/CLAUDE_COMMANDS.md L293**
- 投資基準の空回り検査: `python3 night/audit_deadweight.py`（`--swing`）→ **(A)入力が空で規則が発火しない＝穴**と**(B)値があっても判定が動かない＝本当の無駄**を分けて数える（規則を消すと「測らなかった」事実まで消える） → **docs/CLAUDE_COMMANDS.md L310**
- キルが効いているかの実測: `python3 night/kill_impact.py`（`--all`）→ キルを外したΩを門そのもので出す。
  **原本を読む前に「読んで意味があるか」を測る**道具。初回実測(2026-07-29)では複利停止キル28社すべてで
  キルを外してもΩ75に届かず(最大HD 71.3)、28社とも保有・監視・watchのいずれにも居なかった＝
  **26社ぶんの原本読解はやっても何も動かない**と確定した。キルが効くのは(a)外せばΩ75に届く社がいる
  (b)保有銘柄に立った、のどちらかだけ。買う前に必ずもう一度回すこと
- 「根拠なき値をnull扱いにしたら誰がどう動くか」の実測: `python3 night/shadow_no_evidence.py`
  → 影の計測。パックを退避→根拠なき欄をnull化→score_all→**必ず元へ戻す**。正本の採点は変えない
- 採点式の較正の影の計測: `python3 night/shadow_gmpt.py` → index.htmlの1行だけ差し替えて score_all を回し
  **必ず元へ戻す**。基準を変える前に「誰がどう動くか」を出すための型。他の項の較正を疑うときも同じ型で作る
  （**v9.9.43でgmPtの是正に実際に使った**——欄は営業利益率なのに目盛りが粗利率のまま残り、83%が下2段に
  潰れて priceConfirm が情報を持たず、柱不足ペナルティが93%で発火＝警告として死んでいた）
- 市場データ採取: `python market_fetch.py` → market_data.json（px/per/perF/beta/shy/evebit/analysts/instOwn）
  → `python market_merge.py` でパックのnull市場欄へ機械充填（定性は触らない）→ 門で再取込するとⅥのE[r]判定が生きる
- **門X未評価の解消（実務メモ）**: 門Xは `per>0` が**唯一の必須条件**。PER = 市場株価 ÷ **原本EPS**（純利益÷期末株数・XBRL）で出し、**shy を第二の検算に使う**——per が帯の**下**・shy が帯の**上**へ**逆方向に同時**逸脱したら疑うのは株価ではなく**株数のスケール（株式分割）**。分割は eps・per・shy・mcap を同時に壊す → **docs/CLAUDE_COMMANDS.md L327**
- X監視表の再計算（門X遮断器〔E[r]≥0・v9.9.84〕の開通線+階段指値のfair線）: `python x_watch_recalc.py`（四半期保守で新eps反映後に実行）
- v10影スコア（系列の門・並走検証中）: `python v10_series.py` → out/v10_shadow.json（機械実測5系列70%+定性30%。
  正本の採点・合否には不使用。仕様と切替条件は V10_SPEC.md——2027-07の較正で新旧の予実を答え合わせて勝った方を正本に。
  閲覧ページ: v10.html〔Ⅵ買付順位の「✦v10影スコア↗」または /ccf-gate/v10.html〕）
- 年1回（7月）較正: `python calibration_check.py` → out/calibration.json（門2定性判定の答え合わせ台帳+v9/v10予実。
  erosion/disrupt遷移行列・f1予実。ルーブリック刻みの変更はこの結果を見てユーザー明示指示時のみ）
- 夜間チャンク生成: `python3 night/make_chunks.py`（queue/椅子/棚/backlogの未審査分を10社/枚で追加）
- 月1-2回 採取: `python hachimon_fetch.py`
  - 引数なし=gate1_queue.jsonの未処理上位5社を自動採取。個別指定: `python hachimon_fetch.py NVDA MSFT`
  - 出力: out/{T}_gate_input.json（機械値ドラフト・審査待ち）+ out/{T}_hits.txt（原本キーワード抜粋）
- 8-K日次監視: `python3 night/watch_events.py`（`--days 30`で窓拡大）→ out/events_watch.json。`.github/workflows/events.yml` が平日22:10UTCに自動実行。門の **⚙自動化タブ**が8-K警報＋決算＋カレンダーを一枚で表示（表示専用・判定に不使用） → **docs/CLAUDE_COMMANDS.md L353**
- **運用サイクルの回転盤**: `python3 night/ops_status.py` → out/ops_status.json＝**周期作業14本の最終実行日**を実測し期限超過を「停止疑い」と機械判定（CIは fetch-depth:0 必須）。人の作業と判断の正本は `todo_list.json`（📌やるべきこと）。月次の自動実行は `.github/workflows/ops.yml`（毎月2日23:00UTC・各ステップ continue-on-error） → **docs/CLAUDE_COMMANDS.md L363**
- **門2審査の自動化は GitHub Actions ではなく Routine で回す**（review.yml は鍵が無く一度も走っていない）: 平日05:00 JST・**毎回新規セッション・5社／回・opus5固定・1社ごとに ultrathink**・**並列サブエージェント禁止**（直列で1社ごとに納品検査してコミット）・**mainへ直接書かずPR**。走行ログは `python3 night/log_review_run.py`（結末を6語に分ける。`--push` は作業ツリーを使わず out/review_runs.json **1件だけ**を main へ送る） → **docs/CLAUDE_COMMANDS.md L388**
- **人の決定を repo へ移した(v9.9.131)**: `state.json` ＝ `pf:portfolio`(株数) / `pf:weights` / `pf:sold` / `pf:monthly` / `g7ignite:map` / `g7log:`。**台帳 g7: は含めない**（out/*_gate_pack.json が正本＝同じものが二箇所に正本を持たない）。検査は `python3 night/validate_state.py` → **docs/CLAUDE_COMMANDS.md L435**
- 〔意匠・字数の改版記録は書庫〕v9.9.139 全銘柄に＋株／138 買付順位から保有へ／137 空の段落／136 既定でたたんだ／134 無駄な文字を外した／133 クリップボード／132 手順の帯／130 五彩／111-105 行のグラデーションと色。いずれも表示のみで判定は不変。道具: night/simplify_gate_text.py・fold_gate_text.py・fix_fold_markup.py → docs/CLAUDE_ARCHIVE.md L14700
- **アイコンの色合わせ(v9.9.104)**: `python3 night/logo_colors.py`（`--force`で再算出）→ out/logos/index.json に `c`(代表色)・`d`(暗い地が要る)・`x`(白紙で不採用)。**画素を読むのはCIだけ**（価格と同じ「CIで測ってJSONで配る」作法） → **docs/CLAUDE_COMMANDS.md L445**
- 銘柄アイコン(v9.9.103): `python3 night/fetch_logos.py`（`--all`で取り直し / `--only MSFT,6146`）→ out/logos/{T}.svg|.png ＋ index.json。**外部CDNをブラウザから直接叩かない**。取れない銘柄はモノグラム（「ロゴが無い」と「別の会社のロゴ」を取り違えない）。描画は `ccfIcon()` の単一実装 → **docs/CLAUDE_COMMANDS.md L458**
- 配当分離の採取: `python3 night/fill_divy.py`（`--all`）→ out/divy.json。予実台帳のsnapが観測へ同梱する
- 機械コアの疑似バックテスト: `python3 night/backtest_core.py`（`--cutoff 2015-07-01` / `--limit N`）
  → out/backtest_<year>.json。生存バイアスあり＝相対比較と成長減衰の分布にだけ使う

## 別枠の門（正本ではない・思想不変）
- **門X = chomirai.html（超未来の門）**: 期待リターン最大化。予知せず E[r]=純還元(現金還元−希薄化)+b×ROIIC+
  倍率の重力 の分解機・非対称チェック・複利の漏れ・ケリー(¼上限)を裁く対話式ページ。配信後 `/ccf-gate/chomirai.html`
- 順序は **門Ω(壊れないか審査) → 門X(期待値・サイズ)** の二門。門Xは門Ωの採点・売却規律を上書きしない
  ※怪物の門（点火検知・無知の枠）は「証明前の若い急成長を10%枠で取りに行く」ための別枠だったが、
    無知の枠を運用しない方針のため2026-07に撤去（門Ωが構造的に若い怪物を弾くのは思想通り）。履歴に保存

## 絶対のルール
1. index.html の採点ロジック・売却規律(S1/S2/S3)・採点基準は、ユーザーの明示指示なしに変更しない
2. 採取器の出力は「審査待ち」であり台帳データではない。定性項目(dom/irr/rep/dur/**moatW**/p1-4/f1-5)を勝手に埋めない
   - **dom=支配シェア(v9.9.38改定・2026-07-28ユーザー明示指示)**: 100=独占≥90%(または実質唯一供給) /
     85=圧倒70-89%**または複占の首位**(上位2社で≥80%かつ自社が首位) / 70=寡占40-69%**または複占の一角・3社寡占**
     (上位3社で≥80%) / 50=競争<40%(かつ上記の寡占構造に該当しない)。**シェアの数字だけでは価格支配力を測れない**
     ——10社が分け合う35%と2社で分け合う35%は別物。上位N社の合計シェアで市場構造を必ず確認する。
     **原本にシェア開示が無ければ空欄**(v9.9.39)——空欄は重みを残り4本へ再正規化するので既定値70に化けない
     （従来はパックのdom:nullがSELECT既定70に化け、「測っていない」が「測って70」になっていた）。
     "leading"等の定性表現だけで数字を置くのは憶測＝違反。**空欄の理由は_meta.nulls.domに必ず書く**
   - **domの刻みの決め方(v9.9.41・2026-07-29ユーザー明示指示で決着)**:
     (1) **domは全社ベース**。事業ごとに構造が違っても全てが同じ刻みに落ちるならその刻みで確定し、
         **違う刻みに散るときだけ空欄**（IRMD=ポンプ100/モニタ非首位で割れる→空欄。HEI=FSGもETGも50→50）
     (2) **100/85/70は「集中している」という主張**ゆえ必ず数値（自社シェア%＋上位N社合計%）を要する
     (3) **50は残余の刻みゆえ数値なしでも確定できる**——提出書類自身が「市場はfragmented」「同等品を
         多数が製造可能」「実質すべての供給は他社」と**構造的に**述べていれば40%以上いることは定義上ありえない。
         ここを空欄にすると「測っていない」ほうが再正規化で有利になる抜け道になる
     (4) 「a leader」「largest」等の**順位の自称はどの刻みの証拠にもならない**
     (5) 第三者調査の実数は**対象会社自身の提出書類でなくても**、SEC/EDINET提出書類の中に市場全体の表として
         載り対象会社が名指しで含まれていれば原本扱い可（MAのシェアはVisaの10-K内Nilson表で確定）。
         ただし**どの会社のどの提出書類か**を_metaに明示する
     (6) **発行体自身の断定の実数も、次の4条件を全て満たす場合に限り審査官判断で採用できる**
         （2026-08-04ユーザー明示指示「調べたデータを元にシェアがわかるならそれは入れるべき」）:
         (a)SEC/EDINET提出書類に載る実数である (b)複数の**独立した傍証**（設置台数・構造記述等）と整合する
         (c)反証となる記述がどの提出書類にも無い (d)鮮度が概ね5年以内。
         第1号=ISRG dom=85（SSII提出デッキの85%断定＋傍証3点: 設置台数1万台超vs競合数十〜数百台／
         現地競合最強のインドですら累計約60%／唯一のマルチ専門領域ロボットという構造記述）。
         **RMSL型は引き続き不採用**——RMDで退けたRemSleepの45/30/12は(b)傍証なし(c)Philips退場という反証あり
         (d)8年凍結の定型文、と4条件のうち3つに反する。(6)は「傍証で裏の取れた断定」だけを通す狭い扉であり、
         手技◯%等の**浸透度をシェアと読み替えるのは引き続き違反**（FINDEXの導入率教訓と対称）
   - **moatW=堀の広さ**(v9.9.36新設・深さでなく本数): 100=独立した堀3つ以上(各≥売上15%) / 85=2つ /
     **70=単一事業だが用途・顧客・地域が分散＝残余の刻み(最大製品<70% かつ 最大顧客<20%)** / 50=単一製品・単一用途(1製品≥70% or 最大顧客≥20%)。
     **v9.9.120で刻みの穴を塞いだ(ユーザー明示指示)**——旧規約は70が『最大顧客<10%』で、**顧客10〜20%と製品70〜80%が
     どちらの刻みにも落ちなかった**（実害: 同じ帯の6社が70/50/空欄の3通りに割れていた）。**顧客の10%はASC280の
     開示閾値であって経済的な線ではない**——自ら12%と開示した社が何も開示しない社より不利になる。
     **50をpositiveな主張・70を残余**とし(domの規約(3)と同じ作法)、境界を50側の既存の数字ひとつに統一した。
     新しい定数は導入していない。存続級の依存は別枠のdep(≥40%でベト)が受け持つ。
     セグメント別売上の実数で裏を取る。**根拠が無ければ空欄**——空欄は旧4本式で採点され既定値に化けない。
     **共通の中核技術に全サービスが乗る場合は、顧客・競合・損益が別でも1本**と数える(v9.9.41決着。実例4071:
     有報が「自然言語処理エンジンWatersを全てのサービスに組み込む」と明記→規模上は2本だがmoatW=70)
   - **堀のふるい(v9.9.39・2026-07-29ユーザー明示指示)**: 新規投下は**三段関門**＝Ω75+ ∧ 門X4条件 ∧
     **絶対MOAT指数70+**（線は v9.9.49・2026-07-29 ユーザー明示指示で 75→70）。
     採点式・Ω・売却規律S1/S2/S3はいずれも**不変**——moatIdxは既にΩへ二経路
     (pm→sustain と直接項.13)で入っており、ここで減点すると同じ信号の三重罰になるため「買う対象から
     外す」だけにした。堀不足は**買わない理由であって売る理由ではない**。ふるいは入力の質を超えられない
     ので、納品後は必ず `python3 night/audit_moat.py` で根拠の質を検査する
     （初回実測2026-07-29: Ω75+の25社中◎は1社のみ＝24社が再監査待ち）
   - **堀の関門を75→70へ(v9.9.49・2026-07-29ユーザー明示指示)**: 75は「厳しい」のではなく
     **規約の刻みの空白の中に落ちていた**。5本そろいの堀は 最上段96.0 / 2段目84.0 / **3段目68.4** /
     最下段47.1 で、68.4と84.0の間に刻みが無い。実測の分布も 70-74 が最大の山(59社)で 75-79 は22社＝
     **いちばん厚い山の肩を切っていた**ので、測定の小さな差が合否を決めていた。
     原因は重み上位2本(各.25)の上段が業種によって構造的に届かないこと——
     **irr**: 実測 70=149社/85=54社。85「認証・規格ロック」は*顧客側の再認定が要る型*なので
     決済網・ソフトウェア・ブランド・SaaSは定義上到達できない。
     **dom**: 実測 50=164社/70=98/85+=28/空欄27。85以上は原本内の**数値開示**を要求するので、
     開示習慣の静かな会社は永久に70止まり＝測っているのは堀ではなく開示習慣になる。
     合計50%の重みが上に伸びず幾何平均が68-73へ圧縮され、そこを75で切ると MA・NVDA が「堀不足」になった。
     **70は3段目そろい68.4のすぐ上＝「全部が3段目ではない」**という意味で、規約の刻みと対応が取れる
     （75はどの刻みの組合せとも対応しない数字だった）。実測: 投下可 3社→**7社**
     （+NVDA 73.6 / MA 73.6 / 6857 71.6 / ADBE 71.2。いずれもΩ77-78・門X通過ずみ・堀は原本で再監査ずみ）。
     3923/4071(67.9)・5038(59.2)・8136(59.0)は引き続き落ちる。**採点式・Ω・二本柱・S1/S2/S3・門X閾値は不変**
   - **成長gの実力上限(v9.9.40・2026-07-29ユーザー明示指示)**: 門XのE[r]の g=min(実績cagr, 実力gcap, 20%)。
     gcap空欄時はΩ側と同じ暫定規約「gr(=per/perF−1・1年フォワード)で代用」を門Xにも通す。従来は門Xだけが
     代用を持たず、**実力の推定が無いまま過去のcagrで10年先を描いていた**（全316パックのgcapが空欄＝
     v9.9.31の安全弁は一度も作動していなかった）。`gcap>0` ガードも撤去——0/負値が「上限なし」で素通りし、
     減速を抑える機構が減速銘柄で無効だった。**審査ではgcapを空欄のままにしてよい**（perF更新に自動追随する。
     パックへ数値を焼き付けると陳腐化する）が、空欄の理由は_meta.nulls.gcapに書くこと
3. 銘柄審査を頼まれたら: out/{T}_gate_input.json と {T}_hits.txt を読み、index.html内の審査プロトコル
   （Ⅱ実行手順「3 審査」に全文）に従う。憶測禁止・全判定に原本根拠を付ける
4. companyfacts.zip はコミットしない(.gitignore済)。生成物(gate1_queue.json, out/)はコミットする
5. SEC アクセスの User-Agent メールは設定済み。レート制限(10req/s)を守る
6. 門Xも別枠であって正本ではない。chomirai.html の採点式・閾値は明示指示なしに変更しない
7-b. **米国株ROIC規約（採取器 hachimon_fetch が正・2026-07-29に明文化）**: roic は
   **投下資本 = 自己資本 + 有利子負債 − のれん − 無形／直近年**、NOPAT=営業利益×(1−実効税率)。
   roicg は同じNOPAT ÷ (自己資本 + 有利子負債)。IC が自己資本の2割未満なら**算出不能**（ルール7(b)）。
   **「のれんのみ控除」や「5年平均」は門式ではない。** 2026-07-29に自分でMAを131.0→74.9へ
   「是正」して巻き戻す事故を起こした——同じパックの**古いほうのkenshi(74.9)**だけを読み、
   採取器の頭注が『MAは74.9→131.0の上方修正だった』と19社検算で決着済みと記録しているのを見落とした。
   **台帳の値が食い違って見えたら、まず後から書かれた記録と規約を探すこと。**
7. **欠測をゼロと読むな**(2026-07-29・実測3件で確立)。機械採取・充填のいずれでも、タグが無いことを
   0と見なすと分母が縮退して指標が発散する。同じ事故を3系統で起こしている:
   - hachimon_fetch の投下資本: 有利子負債タグ不在→debt=0→IC=自己資本−のれん−無形へ縮退。
     買収で伸びた会社は自己資本の大半がのれん＋無形なのでROICが発散(実測 NJR 16.5→真値6.6・
     負債3.6十億$欠落 / HEI 93.0 / APH 163.4)。日本株で廃止した旧・門式と同型
   - JP ROIC旧・門式: 投下資本−過剰現金でネットキャッシュ企業の分母が0へ縮退(実測957.7%/502.6%)
   - market_merge: 常識帯の関門が採取側にしか無く、market_data.json に残った旧不正値が何度でも
     パックへ再注入されていた(実測11社。KLAC shy=12.28 / V 13.27 / HCKT 31.8 等)
   **原則**: (a)「タグが無い」と「値が0」を区別する (b)分母が自己資本の2割を切ったら算出不能として
   null化し理由を_metaに書く (c)**保管された値も毎回検問する**——採取器を直せば安全、は誤り
   〔事故の目録は書庫〕同族4件(候補タグの先頭を無条件採用／年数を要素数で数えた／dilNetが分割をまたいだ／取れた値＝最新の値)・五つ目(CELHの無期限無形)・六つ目七つ目(KLACの営利タグ消滅／IRMDの無借金で算出不能)・販管費の分割報告 → docs/CLAUDE_ARCHIVE.md L14968
   **「候補＝代替」と「候補＝構成要素」の取り違えを、無形・有利子負債・販管費と同じセッションで3度踏んだ**
   ——`series()` を呼ぶ前に毎回「この候補群は代替か、構成要素か」を問うこと。

7-c. **ROIC は through-cycle＝5年の中央値（v9.9.72・2026-08-03 ユーザー明示指示
   「今の12社に忖度するのではなく本当に必要なものを採用したい」）**——米国式・JP式に共通。
   `roic = roic系列の中央値` ／ `roicg = roicg系列の中央値`（同じ年・同じNOPATから対で作る）。
   中央値どうしなら系列が点ごとに `roic_y ≥ roicg_y` なので**恒等式が順序統計量として保たれる**。
   **片方だけ基準を替えてはならない**——NOPAT>0では roic≥roicg が恒等式なので、
   数学的に不可能な組合せができる（KLACの分割・ADRのper・JP門0のptと同じ「基準の違う二つを割る」型）。
   - 〔経緯は書庫〕なぜ重みでなくここを直したか／なぜ中央値か／min(直近,中央値)を採用して撤回した（忖度の記録）／実測105社・投下可12→11(APH脱落)／acq5誤り3社／比は「パックが審査した年」で取る／在庫の数え方の誤り → docs/CLAUDE_ARCHIVE.md L15028
   - **当てる範囲**: 審査年で採取器とパックが**15%以内で一致する社だけ**（181社中105社）。
     審査官が採取器の値を棄却して書き直した社（実測 GEN 107.9→15.5 / CHH 51.9→19.9 / RBA 56.5→10.6）は
     **5年系列も同じ縮退を含む**ので比が意味を持たない。触らず作業リストへ（誤値より空欄）。
     恒等式が壊れる社（実測MPTI）も同じく触らない——clampで黙って直すと審査官の判断を機械が上書きする
   - **これがいちばん効く実例**: 同じ半導体でも **アドバンテスト(6857) は 直近46.6 / 中央30.7 / 最悪13.0**、
     **ディスコ(6146) は 22.8 / 22.8 / 21.7**。単年では両方「高ROIC」に見えるが、
     through-cycle で初めて**まったく別の事業**だと分かる
   - 在庫 `out/tc_roic.json`（**181社**・年つき系列 series={会計年:[roic,roicg]}。
     US179社=採取器 build_numbers の `_tcSeries` をそのまま使用／JP2社=EDINET_DBからJP規約で算出し、
     パックの審査済み値と**完全一致**を検算済み）。
     影の計測器は `python3 night/shadow_tc_roic.py`。
     **Ω・採点式・売却規律S1/S2/S3・門X閾値・堀の関門・四段関門はいずれも不変**
8. **値を書くなら根拠も書く**(2026-07-29新設)。値のある採点項目は _meta.evidence に対応する記述が要る。
   実測(night/audit_evidence.py・全316パック)で根拠被覆率は**33.0%**、機械項目は**9.8%**しかなく、
   **原本から測った値とそれらしく置いた値が台帳上まったく同じ見た目**になっていた。これがルール7の事故が
   長く見えなかった理由——注意力ではなく、**数字を作るようには設計されていたが証明するようには
   設計されていなかった**という構造の問題。
   - 採取器は機械項目に**式と実額**を自動で刻む（_meta.evidence + `_meta.provenance[欄]="machine"`）
   - market_merge は充填した市場欄に「いつ・どこから」を刻む
   - **審査官は provenance:"machine" の欄を黙って上書きしない**。検算して直すのは可だが、
     その場合は _meta.kenshi に「旧→新・原本の実額・出典」を書き evidence も自分の実測へ書き換える
   - 空欄の理由は _meta.nulls に書く。理由の無い空欄は差し戻し
   - 納品前に `python3 night/validate_packs.py {T}` を通しFAILゼロにする

## 月次運用（ドルコスト平均・積立）
毎月の定額入金を機械的に投下する。正本の原則「買うリズム(毎月・自動)と選ぶリズム(年1回・手動)を分ける」の実装。
**DCAは「いつ・いくら」を決め、門は「どれを・買うか否か」を決める**（役割が別＝門を上書きしない）。
- **配分（動的）**: 網(ETF)と城(個別)へ、目標ウェイト(**網50/城50**・v9.9.68で城40%→60%へ改定 → 2026-08-19 ユーザー明示指示「50%ETFへ」で50/50)に対し**不足している側に厚く寄せる**＝新規資金だけで
  リバランス(売却・課税なし)。**この網/城の判断は門の外（DCA側）で行う**——v9.9.53(2026-07-30・ユーザー明示指示)で
  門の「今月の買付プラン」から網を撤去した。**門は個別銘柄のための門**であり、指数を何円積むかは門の裁きではない。
- **網(ETF)＝無条件で即買い**: DCAの土台。下落月ほど口数を多く拾い高値づかみを自動で薄める。門にはかけない
  (指数は常に買ってよい対象)。証券会社の自動積立で機械的に。
- **城(個別)＝投下可にだけ。投下可＝四段関門∧合成点上位10社(第五の枠・v9.9.88・2026-08-05
  ユーザー明示指示「上位10社を買い付け可にして」)**。四段は資格・第五は席数。判定は
  **ccfAllocTop（単一実装）**をⅥ・盤・score_all・予実snapの4検査器が共有する(v9.9.65の掟)。
  11位以下は**🔵次点**＝買わないが資格は保持・合成点が上がれば自動昇格(黙って消さない・v9.9.52)。
  点灯(Ulysses冷却)も枠内のみ。Nの感度は実測済み——**8社が硬い床**(上限8%×8=64%でようやく城60%が
  埋まる。7社以下は城が埋まらず総合が落ちる)、算術ピークは8社(13.87%)だが10社(13.63%)との差0.24ptは
  E[r]推定誤差より小さい**同じ台地**。10社を採る理由: 保有RMD/6146が枠に残る・推定誤差への分散・
  一発の毀損の尖り緩和。12社以上は低E[r]で薄まり明確に劣化(18社=12.60%)。
  配分は(v9.9.87・「質の良さと加重のE[r]のバランスで得点をつけては」):
  目標ウェイト＝城60% × 合成点÷Σ合成点(上位10社)・上限8%。**合成点=(Ω−70)×E[r]点÷50**
  （E[r]点は合成順と同じ錨12%=50点・±10ptで0-100＝**新しい定数を発明していない**。掛け算=幾何平均の思想。
  E[r]≤2%は自動でウェイト0＝遮断器(E[r]≥0)と連続につながる）。
  【なぜ】遮断器化(v9.9.84)で投下可が18社に増えたが配分式が(Ω−70)按分のままだと旧ハードル12%未満の
  7社(V/IDXX/SAP/MCO/CDNS/MSI/RELX)が城の23ptを受け取り**総合11.2%へ低下**していた（影の計測3案:
  現行11.20／合成のみ12.60／E[r]≥12で切る13.28だが半導体30.4ptへ再集中）。
  【実測】合成点上位10社は**遮断器化以前の旧・門X4条件の投下可10社と完全同一**
  (ASML/KLAC/MSFT/RMD/IRMD/6857/NVDA/6146/MA/ADBE)＝独立な二つの道が同じ集合へ収束。
  加重E[r]16.71%・総合13.63%。11位以下の投下可は**「配分枠外」と名指し表示**（投下可の資格は保つ＝
  順位が上がれば自動で枠入り。黙って消すとv9.9.52型）。半導体連鎖31.5pt=城の53%は相関上限の決断待ちのまま。
  Ⅵ買付順位の「投下可」(Ω75+ ∧ 門X遮断器〔E[r]≥0・v9.9.84〕 ∧ 絶対MOAT指数70+ ∧ 点検0件)へ、目標との不足分で按分
  (B案②＝実装どおり。実質は不足上位に厚い。pc順位=買う順・gap按分=金額の二序列)。指値は階段（1/2をX開通ライン・1/2をfair線=倍率の重力ゼロ線。2026-07改定＝約定期待値12%錨付けの是正）。1銘柄の目標ウェイトは **城60% × (Ω−70) ÷ Σ(Ω−70)、上限8%**（v9.9.70・2026-08-03 ユーザー明示指示で**段差8/5/3から連続へ**）。旧式は Ω80/77/75 の線で割れ、**IRMD 80.1 と MSFT 79.1 の1.0pt差で配分が1.6倍**違っていた——門の本文Ⅰ「ティア内の小数差に意味はなく、選別は間（価格）が行う」と正面から矛盾していた。実測（城60%へ正規化・網9%）: 旧 3.0〜7.9%/総合13.56% → **新 3.3〜7.6%/総合13.63%**＝リスクの取り方をほぼ変えずに境界の恣意性だけを除いた。**上限8%（¼ケリー相当）は維持**——比で配ると銘柄数が減ったとき1銘柄が過大になるため（投下可5社なら1社12%）。刻みの候補も実測した: (Ω−74)は1.8〜10.1%で幅が開きすぎ、(Ω−74)²は0.5〜16.5%で¼ケリーを大きく超える。**質(Ω)とE[r]は逆相関している**（ASML Ω82.8でE[r]15.0% / ADBE Ω76.4でE[r]23.6%）ので、質を重視するほど加重E[r]は下がる（等加重17.21% / (Ω−70)16.71%）。それでも質で張るのは、20年の複利では**1回の恒久的な毀損が20年ぶん複利で効く**から。
- **投下可がゼロの月**: 個別枠は建てず**網に回す**(1〜2ヶ月開かなければ網へ。開いたら新規資金を城へ戻す)＝現金を
  遊ばせない。個別が割安な時は城が育ち、割高な時は網が育つ自己修正的な呼吸。
- **買付の直前に `node night/score_all.js` を回す**(2026-08-03新設)——🟢投下可の各行が**点✓（未解決0）**である
  ことを確認してから注文する。門(ブラウザ)は _meta を読めないため**要修正(err)しか裁けない**。
  未解決warn（根拠なき値）の関門は端末側にしかないので、この1手を挟まないと四段関門の第四が半分しか効かない。
- **売りは不変**(S1/S2/S3・株価では売らない)。DCAは下落でも買い続けるので規律と整合。
- **個別枠の入力(v9.9.51→v9.9.53・2026-07-30)**: Ⅵ買付順位の「今月の買付プラン」に**今月の個別枠**欄がある。
  `17万` / `170,000` / `170000` / `17万5000` のどれでも受ける（読めない入力は未入力と同じ＝%表示へ戻す。勝手に推測しない）。
  入れた金額は**全額が投下可の各社へ配られる**（網へ回る分を差し引かない）。保存先は localStorage `pf:monthly`。
  **金額は配分の比率に一切触れない**——各社の「目標ウェイト−現在保有%」の不足按分(B案②)に掛けるだけ＝表示層のみ。
  100円単位で切り捨て、**端数は買付順位1位へ**寄せて合計を個別枠にぴったり一致させる
  （v9.9.51では端数を網が吸っていたが、網を撤去したので吸収先をpc順位の先頭にした）。
  各行には**階段指値の2段分**（1/2ずつ）も円で出る。採点式・Ω・売却規律S1/S2/S3・門X閾値・堀の関門はいずれも不変。
  **v9.9.51からの変更点(v9.9.53)**: 入力欄の意味が「今月の入金**総額**」→「今月の**個別枠**」。
  17万を入れると、旧: 網¥102,300＋城¥67,700 / 新: **9社へ¥170,000全額**。網の行は箱から消えた

## 並走運用（重要）
現行の正本系は Google Drive + Colab + Cloudflare。このリポジトリは並走検証中の新環境。
台帳・保有の記帳は現行側のみ。ここの出力は現行と突き合わせて一致を確認するのが仕事。
