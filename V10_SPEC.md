# CCF Ω v10「系列の門」仕様書（2026-07制定・影スコア並走中）

> **状態: 影（シャドー）**。正本はv9.9.x（index.html）のまま不変。v10は out/v10_shadow.json に
> 並記されるだけで、採点・合否・売却・買付順位には一切使わない。
> **切替判定: 2027-07の年次較正**（calibration_check.py）で新旧の予実を答え合わせし、
> 勝った方を正本にする。検証なき基準改定はしない——それがこの門の最大の強み。

## 設計思想（v9からの転換は一つだけ）

**「点」でなく「実測系列（傾き）」で測る。**
2026-07の実証精査で確定した事実に基づく:
- 判別力が最も高い審査項目は実測ベースのf1（P(2社が異なる)=0.88）——実証値ほど選別力が高い
- 主観ルーブリック12項目のうち序列化に寄与する独立次元は実質4つ。nrrは定数(0.01)、f5/p3は写像
- p2（不況耐性）は280パック中59.3%が85点以上・根拠の9割がCOVID一本足＝甘い
- 壊れる会社は水準が高いまま「傾き」が先に死ぬ（Intel2015型）——点の採点では原理的に見えない

精度の敵は項目の少なさではなく**答え合わせの不在**。v10は項目を増やさず、
機械実測に寄せ、較正ループで毎年自分を修正する。

## 採点構造

**合成 = 幾何平均（弱点を隠せない・v9と同じ）**

### 第一層: 機械実測5系列（70%・XBRL年次系列から全自動＝毎四半期更新可能）

| 系列 | 重み | 測るもの | アンカー |
|---|---|---|---|
| ROIIC系列 | 20% | 3年ローリングΔNOPAT/ΔICの水準×傾き | ≥24%→90 / ≥17→80 / ≥12→70 / ≥9→60 / 未満→45。傾き上昇+5・低下−8。ΔIC≤0（還元しながら成長）→75 |
| 増分マージン系列 | 15% | ΔOP/ΔRev(3年)と現行OPMの比較 | >OPM×1.1→90 / ≥×0.8→75 / ≥×0.5→55 / 未満→40。**2期連続マイナス→35＋要審査フラグ**（堀劣化の機械的先行指標） |
| through-cycle実測 | 15% | 2008-09/2020/2022-23の営利DD実測（最悪値） | ≥−10%→95 / −25%→85 / −40%→70 / −60%→50 / 超→35。不況実測なし→上限70（p2のCOVID一本足を排除） |
| 希薄化調整1株FCF成長5年 | 15% | FCF/希薄化後株数のCAGR＝経営の通信簿（買収・還元・希薄化を1本に集約） | ≥20%→95 / ≥15→85 / ≥10→75 / ≥5→60 / ≥0→50 / 負→35 |
| 粗利安定×転嫁力 | 5% | 粗利水準・σ5年・2021→23の転嫁実績 | 水準50%+→80基点、σ<1.5pt→+10 / >4pt→−10、21→23転嫁維持→+5 |

### 第二層: 定性（30%・情報量が実証された独立次元のみ。影期間は既存パックから自動導出＝審査負担ゼロ）

| 項目 | 重み | 中身 |
|---|---|---|
| 堀1因子 | 18% | dom/irr/rep/durの加重幾何平均(.35/.25/.20/.20)＋方向調整（erosion emerging−6 / active−15、disrupt threat−12）。切替後は「堀の型と方向」の1問に統合 |
| 資本配分 | 7% | 基点75。のれん込みROICが事業ROICを8pt超下回る→−10、SBC≥12%→超過分減点。切替後は創業者/オーナー・インサイダー恒常保有%を一次データで加点 |
| geopol | 5% | 0→95 / 1→80 / 2→60 / 3→40（独立次元と実証済み） |

### 第三層: 合否ゲート（現行維持・v10は変えない）

キル群・三本柱≥70・ティア80/72・投下閾値75・門X4条件・S1/S2/S3。
erosion/disruptは**採点でなくゲート専用**（希少イベント検知を序列に混ぜない）。

## やらないこと（門の確定判断の尊重）

低PER選別・フォワードPER・モメンタム・インサイダー売買タイミング・無知の枠。
**最大スキューの事前捕捉は狙わない**——右裾は掟三（勝者を売らない）＋第二S字検知
（kessan_checkの吉S字を審査トリガーに）で取る。採点の役割は左裾を切り中央値を守ること。

## 較正ループ（精度の本当のエンジン）

毎年7月: `python v10_series.py`（影更新）→ `python calibration_check.py`
（v10影スコアもスナップショットに保存済み）→ 前年判定との突合で
「erosion見逃し率・f1/ROIIC系列の予実・v9とv10の予実勝敗」を測る。
**予実が改善しない項目は翌年重みを削る（因子の狩り）**——これを唯一のルーブリック
改定手続きとする（実施はユーザー明示指示・絶対のルール1）。

## 切替時にやること（2027-07にv10が勝った場合のみ）

1. Ⅲ採点機にv10計算を実装（機械5系列はv10_series.py出力を取込）
2. 依頼文の定性を4問化（堀の型と方向・資本配分・geopol・erosion/disrupt〔ゲート用〕）
   ——**現行採点機はp1-4/f1-5を必要とするため、切替前の4問化は正本を壊す。順序厳守**
3. night/テンプレ同期・台帳の互換注記（v9記録は読み取り専用比較）

## 初回実測（2026-07-27・監視28社）

上位: RMD 88.5 / TDG 88.0 / IDXX 85.4 / LMAT 83.9 / FSS 83.6
下位: KLAC 62.4 / LRCX 65.9 / ASML 67.3 / ANET 68.7（循環のROIIC系列を正しく罰する）
現Ωとの主要乖離（＝2027較正の答え合わせ材料）: ASML（Ω81.8 vs 影67.3・through-cycleの
実測が重い）、TDG（Ω38.5 vs 影88.0・v9はキル/レバレッジで殺し、影は系列だけを見る——
**ゲートを通さない影の限界であり、合否は必ず現行ゲートが裁く**）。
