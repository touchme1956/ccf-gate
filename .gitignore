companyfacts.zip
__pycache__/
*.pyc
out_gate0*.log
out_*.log
night/*.log
# --- APIの鍵（絶対にコミットしない・2026-07-29に網羅化）------------------------
# このリポジトリは GitHub Pages で配信＝**公開**なので、鍵を1度コミットしたら
# 履歴に残り、削除しても第三者は取得できる（＝鍵の再発行が必要になる）。
# market_fetch.py が探す場所は ./〜 / ./ccf/〜 / /content/drive/MyDrive/ccf/〜 と
# 環境変数 AV_KEY / FMP_KEY。ファイルで置く場合はここで除外しておく。
*_key.txt
*_key.json
av_key.txt
fmp_key.txt
edinet_key.txt
.env
edinet_csv/
edinet_codelist.zip
edinet_docindex.json
out/_cf_cache/
out/_*.log
# out/_sic_cache.json — 2026-08-10に**追跡へ戻した**。
#   キャッシュだが (a)約2,900社×小さなJSONで数百KB (b)**増分**で作るので
#   コミットしておくと毎月の再構築が新規上場ぶんだけで済む
#   (c)端末で fill_sht をすぐ回せる＝shtの作業リストがいつでも出せる。
#   無視したままだと毎回6分かけて作り直すことになり、しかもローカルでは誰も持っていない。
scratchpad/
out/score_all.partial.json
out/v11_facts.partial.json
out/backfill_diff.partial.json
out/_roic_series_cache.json

finnhub_key.txt
out/_price_commit_msg.txt
out/_subs_cache/
out/_retro_docs/
out/_histval_cache/
out/_histval_lookahead_cache/
out/_partial_hist_val_*.json

# night/check_mobile_fit.js が要る playwright（CIには入れない・手元で `npm i playwright`）。
# 100MB級なので `git add -A` で誤ってコミットしないよう除外しておく。
node_modules/
package.json
package-lock.json

# 退場社の採取キャッシュ（night/retro_delisted.py・再実行で再生成できる）
out/_delisted_cache/
out/_delisted_cache/
