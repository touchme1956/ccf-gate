# CCF Ω v11「引き算の門」仕様書（2026-08-12制定・影スコア並走）

> **状態: 影（シャドー）**。正本は v9.9.x（index.html）のまま**一字も変えない**。
> v11 は `out/v11_shadow.json` に並記されるだけで、採点・合否・売却規律・買付順位・配分には一切使わない。
> **これは「作り替え」の実装だが、正本の置換ではない。** 置換はユーザーの明示指示の領分（絶対のルール1）。
> **判定基準は下の「切替の条件」に、測る前に書いてある**——v10 が「勝った方」の定義を持たないまま
> 1年並走して後から判定式を足した（CLAUDE.md 2026-08-04）のと同じ穴を作らないため。

---

## なぜ作るのか——研究が出した答えは「何を足すか」ではなく「何が効いていないか」だった

2026-08 の一連の検証（勝者／破壊の対称探索・複利10%の5角度・自己相対バリュエーション・dd5・
価格の遮断器・irr=85 の3ビンテージ追試）で確定したのは、次の6点である。**すべて事前登録つきで測った。**

| 実測 | 出典 |
|---|---|
| 入口の機械値で「勝者」を当てる指標は**7回探して合格ゼロ** | `hist_wd_*` / `hist10_*` / retro_winner_profile / retro_capture / retro_capture2 |
| 「潰れる会社」を当てる指標は**実在する**（RR 3〜5倍・置換 p=0.0005）が、**門が既に全部捕まえている**（増分 0.0／irr≥70 の層内に破壊社ゼロ） | `hist_wd_verify_destroy.py` |
| **効いた変数はただ一つ、移行障壁の型（irr）**。50: 0.162 → 70: 0.366 → 85: 0.579。**主観の堀(moat5)は lift +0.02**、dom≥70 は **P=0.00** | 2013/2015/2018 の読解 754件 |
| **irr=100 は3ビンテージとも最下位**（0.056 / 0.091 / 0.000）＝刻みの順序そのものへの反証 | 同上 |
| 財務の左尾を分けるのは**残高倍率ではなく金利負担**。nde は門自身の線で**非単調**（3-4帯0.06 vs 4-5帯0.03）、intcov は単調（≤1 0.14 → ≥5 0.02） | `retro_nde_kill85.py` |
| 価格・期待値の線は質・堀の中で**選別力を持たない**（横断面PER・fairPER倍率・E[r]・自己相対分位・dd5 の5例） | `hist_val*` / `retro_er_test2.py` |
| **ρ(事業の寄与, 倍率の寄与) = −0.96 / −0.88** ＝総リターンを目的に置くと、事業側の信号は倍率で相殺されて見えなくなる | `hist10_angleC.py` |

**v11 の主張は加算ではなく引き算である。** 「新しい指標を見つけた」とは一言も言わない。言うのは
**「一度も効くと示されていない機構を、門から降ろす」**だけ。この主張は加算より弱く、**そのぶん検証可能**である。

---

## 設計（4層）

**層は掛け算ではない。順に通る関門であり、点を合成しない。**
v9 の Ω は 11 欄の入れ子幾何平均で 1 つの数を作るが、実測（`audit_weights.js --q75`）では
判定帯でばらつく欄は roic ただ一つ（変動係数 64.0% に対し p4 7.9% / f1 6.4% / 堀 9.9%）＝
**残りは定数で、合成しても順位を作っていない**。順位を作らない数を合成する機構は持たない。

### 層0 — 土俵（機械・二値）

```
営業利益率 ≥ 10%  ∧  5年すべてFCF黒字  ∧  5年すべて営業利益黒字
```

- **点を作らない。名簿を作るだけ。** ここの数字は次の層へ持ち越さない。
- 由来は発明ではなく、歴史検証が全編で使ってきた「質実証プール」の定義そのもの（`retro_moat_durability.py`）。
- **この層は選別しない**（質実証だけでは P(15%+) 0.213 vs ベース 0.221＝リフトゼロ、と実測済み）。
  役割は**測れる場所へ絞ること**。実際、質実証プールでは恒久毀損が 8%→6% に下がる。

### 層1 — 機構（原本・3段）

**問いは一つだけ:「この会社を外すとき、費用を払うのは顧客か、当社か」**

| 段 | 条件 | 歴史の P(継続) |
|---|---|---|
| **A** | 顧客の側が**再認定・再試験をやり直す**（原本に断定形の記述がある） | **0.579** |
| **B** | 移行に摩擦はあるが、費用を払うのは当社側 | **0.366** |
| **C** | どちらでもない | **0.162** |

- **A/B/C は v9 の irr=85/70/50 と一対一**。刻みも根拠の要求（原本の引用・向きの検問）もそのまま。
  ＝**較正を捨てていない**。改名は「何を測っているのか」を一行で言えるようにするためだけ。
- **irr=100 は作らない。** 実測で3ビンテージとも最下位（規制公益の排他的フランチャイズが大半で、
  認証ロックとは別の機構）。加えて v9 では領域独占が **irr と dom の両方**で数えられ、
  **合計 .50 の重みが一つの事実から出ていた**（2026-08-06 の全数精査(D)）。
- **dom・rep・dur・moatW は採点欄から降ろす。** 記録は残す（原本の資産だから）が、**合否の重みを持たせない**。
  根拠: moat5 の lift +0.02、dom≥70 の P=0.00、判定圏の irr は 45/49社が70で変動係数 6.6%＝定数。
  そして **dom 空欄の再正規化は『dom＝他4本の平均』と仮定するのと同一**で、平均 +7.1pt の下駄になっていた。
  **欄を消せば下駄も消える。**

### 層2 — 壊れない（二値の関門・5本）

| 関門 | 線 | 由来 |
|---|---|---|
| 存続級依存 | `dep ≥ 40%`（相手が主権政府なら免除） | v9.9.85/121 をそのまま |
| 粗利の侵食 | `erosion = active` | v9 の欄をそのまま |
| 破壊の脅威 | `disrupt = threat` | 同上 |
| 事業の収縮 | `売上5年CAGR < 0 ∧ 営業利益率が5年で低下` | v9.9.99 をそのまま |
| **金利負担** | **`利払カバー < 3`** | **← v11 で唯一替える物差し** |

- **nde（純有利子負債/EBITDA）のキルを廃し、intcov（利払カバー）にする。**
  根拠は上表のとおり：nde は門自身の線で非単調（借金が 4.00 を跨ぐと Ω が **+8.7pt 上がる**区間が
  v9.9.126 まで実在した）。intcov は単調で、切ったときの濃縮も 3.5倍 vs 1.5倍。
  **線を3にするのは「1〜3帯で毀損が跳ねる」実測から**（≤1 0.14 / 1-3 0.05 / 3-5 0.05）で、
  5 で切ると止める数が2倍以上に増え「ただの間引き」になる（質実証プールでの実測）。
- **測れないものは通さない。** intcov が算出できない社は「安全」ではなく「未測定」＝
  `判定不能` として土俵から降ろし、**理由を名指しで出す**（絶対のルール7）。
  ⚠ 日本株は SEC 経路に居ないので構造的に未測定＝**穴として明示する**（v11 の最大の未解決点）。

### 層3 — 期待値（記述のみ・合否にも順位にも使わない）

- E[r] は計算して**表示する**が、関門にも席順にも使わない（v9.9.98 で既にそうなっている）。
- **成長は最初から減衰させる**（`g = 実証5年 → 5年後から終端6%`）。
  根拠: trailing 20-30% で入った社の前方10年は**中央値7.1%・15%維持は20%**（`retro_growth_persistence.py`）。
  v9 は g=min(cagr,20) を20年据え置くので、**予測が構造的に甘い**。
  ただし**これは合否を動かさない**（層3は記述）ので、賭けているのは「記録の正直さ」だけ。
  予実台帳（`audit_er_realized.py`）が2027-02から答え合わせできる。

### 層4 — 席

- **順位を作らない。** 層0-2 を通った社は**同格**。等ウェイト。
- 席が足りないときの順は **(1) 機構A を先 → (2) 通り続けている期間が長い順**。
  「点の高い順」を使わない理由: 判定帯のΩのσは 2.30pt しかなく、**誤値の是正1件で12pt動いた実例**
  （ISRG）がある＝**増幅しているのは信号ではなく測定誤差**。
- テーマ上限（半導体連鎖は席の30%）は v9.9.117 をそのまま。
- 売りは **S1/S2/S3 のまま一字も変えない**（絶対のルール1）。

---

## 捨てるもの / 変えないもの

**捨てる**: Ω という合成値そのもの ／ p1-p4・f1-f5（11欄のルーブリック）／ 二本柱の崖とティア ／
irr=100 の刻み ／ dom・rep・dur・moatW の**重み**（記録は残す）／ nde の財務キル ／ 席の点順。

**変えない**: 原本優先・引用必須 ／ 誤値より空欄（ルール7）／ 事前登録つきの較正 ／
「買わない理由 ≠ 売る理由」／ 20年降りない ／ 走行ログと回転盤 ／ S1/S2/S3 ／
**そして irr の刻みと根拠の要求**（＝門で唯一、効くと実証された機構）。

---

## 切替の条件（**測る前に固定する**・2026-08-12）

v11 が影から正本へ上がるには、**次の4つをすべて満たす**こと。一つでも欠ければ**影のまま**。
満たしても切替はユーザーの明示指示（絶対のルール1）。

1. **層ごとに増分がある**。歴史パネル（`out/hist_wd_panel.json`・2016/2017/2018 の3ビンテージ）で
   層0 → 層0∧1 → 層0∧1∧2 と重ねるごとに、**(a) 恒久毀損率が上がらない** かつ
   **(b) 実現年率の中央値が下がらない**。**増分ゼロの層は v11 自身から落とす**
   （「働いていない規則は消す」を自分に当てる）。
2. **財務の物差しの交換が支持される**。2018年ビンテージ（nde が採れる唯一の窓）で、
   intcov<3 の関門が nde>4 のキルより **(a) 止めた側の恒久毀損の濃縮が高い** かつ
   **(b) 止めた側の実現中央値が通過側より低い**（両方向とも正しい）。
3. **削除が支持される**。降ろした欄（dom/rep/dur/moatW＝歴史側は `moat5` と `irr_near` 以外の堀情報で代理）を
   v11 に戻したとき、恒久毀損も中央値も**改善しない**こと。改善するなら削除は誤り。
4. **今日の台帳で誰がどう動くかを実測し、名指しで出す**。v9 と v11 の投下可の差分を
   一社ずつ理由つきで示す（黙って消さない・v9.9.52）。

**限界を先に書いておく（これは不合格の言い訳ではなく、判定の射程の宣言）:**
- 歴史パネルに **Ω は再構成できない**（定性11欄が当時の原本で採点されていない）。
  よって比較できるのは**両門の機械部分だけ**であり、「v11 が v9 より良い」は
  **機械部分についてのみ**言える。CLAUDE.md が retro_cohort について書いた限界と同じ。
- `dep` / `erosion` / `disrupt` は歴史側に無い＝層2 の5本のうち**3本は歴史で検定できない**。
  これらは v9 から持ち込んだ既存の関門なので「v11 の新規主張」ではないが、増分は測れない。
- 3ビンテージは**同じ 956 ティッカー**（Jaccard 1.00・返り値ρ 0.845〜0.967）＝
  **out-of-sample はゼロ**。窓は 2018-2026 を共有し AI 相場を含む。
- 恒久毀損は基準率 5.8〜8.1% なので、**「破壊を減らす向き」の絶対差は数学的に到達しにくい**。

---

## 道具

| | |
|---|---|
| `night/v11_intcov.py` | 利払カバーを原本(SEC XBRL)から採る。**歴史側と一字一句同じ定義**。出力 `out/v11_intcov.json` |
| `night/v11_gate.js` | v11 の**単一実装**。今日の全パックへ当てて `out/v11_shadow.json` を書き、v9 との差分を名指しで出す |
| `night/v11_backtest.py` | 上の「切替の条件」1〜3 を歴史パネルで実測する。出力 `out/v11_backtest.json` |

**判定は `v11_gate.js` の `v11Gate()` ただ一つが持つ**（写さない・v9.9.65 の掟）。
backtest は同じ規則を歴史の欄名へ写像するが、**線と刻みはこの仕様書の表から読む**。

---

## 実装中に判った欠陥と、それを受けた改訂（2026-08-12・**すべて結果を見た後**）

**この節の改訂は out-of-sample ではない。** 測ってから直したものは、直した後の数字を
「当たった」と呼べない——`hist10` で自分に課したのと同じ規律をここにも当てる。

### ① 「測れないものは通さない」は、そのままでは**被覆のふるい**だった（層2）

歴史パネルで層2が落とした81社の内訳を数えたら——**未測定（利払カバーが採れない）68社／
線(intcov<3)で落ちた7社／事業の収縮6社**。**未測定の68社に恒久毀損は0社**。
つまり層2は、この母集団では**リスクのふるいではなく被覆のふるい**として働いていた＝名前を偽っていた。

原因は明白で、**利息の年次タグが無い社の大半は無借金**である。
→ 改訂: **「採れない」と「無い」を区別する**。有利子負債・利息の痕跡タグが一つも無ければ
**無借金を事実として通す**（採取器 `hachimon_fetch.debt_evidence()` が確立した「痕跡ゼロのときだけ 0 を
事実とする」＝上限の不等式。新しい作法ではない）。痕跡があるのに利払カバーが採れない社だけを止める。

**⚠ この改訂の代金も測ってある**: 未測定を通すと pooled で n 115→181・中央値 10.07→9.63%・
最悪 −8.2→−14.9%（恒久毀損はどちらも0）。**通したほうが最悪値は悪くなる。**
それでも通すのは、拒否の理由が「危ない」ではなく「うちのデータ経路に無い」だったから
——**日本株は SEC 経路に無いので、拒否を続けると層2は事実上の国籍フィルタになる。**

### ② データ健全は層0に属する（明文化）

`validate_packs` の FAIL・期末後の重大事象・未完了の重大事象は、v9 から**そのまま持ち込む**。
これは採点の話ではなく「**パックが会社の現在を描いているか**」の話なので、層0（土俵）に置く。
v11 の新しい主張ではないので、切替の条件では検定しない。

### ③ 層4 の席順は**今日の repo では作れない**（未解決点として記録）

「通り続けている期間が長い順」には**門の判定の履歴**が要るが、この repo は
`out/score_all.json` を毎日上書きするだけで**過去の合否を保存していない**。
＝v11 は「点で並べない」と言えても、**代わりの並べ方を今日は実装できない**。
影では**通過した集合だけ**を出し、順位は付けない（`seat_order: 未実装` と明記して出力する）。
これは設計の弱点であり、隠さない。
